*-----------------------------------------------------------
* Title      : TESTING CODE OF SUB, SUBA, AND SUBI COMMAND
* Written by : A BISCUIT
* Date       : 5/22/2015
* Description: 
*-----------------------------------------------------------
        ORG    $1000

START:
*-----------------------------------------------------------
* **********************  SUB TEST  ************************                                                
*-----------------------------------------------------------

*------------ FIXED DESTINATION: SOURCE OPERAND ------------

        SUB.B   D0, D1                  ;SUBTRACTING D0 FROM D1, THEN STORE RESULT IN D1
        SUB.B   (A0), D1                ;SUBTRACTING DATA THAT A0 POINTS TO FROM D1 AND STORE RESULT IN D1
        SUB.B   (A0)+, D1               ;SUBTRACTING DATA THAT A0 POINTS TO FROM D1 AND STORE RESULT IN D1, THEN INCREMENT THE POINTER
        SUB.B   -(A0), D1               ;SUBTRACTING THE POINTER, THEN ADDING DATA THAT A0 POINTS TO FROM D1 AND STORE RESULT IN D1
        SUB.B   $4000, D1               ;SUBTRACTING DATA AT THE GIVEN ADDRESS FROM D1 AND STORE RESULT IN D1
        SUB.B   $00004000, D1           ;SUBTRACTING DATA AT THE GIVEN ADDRESS FROM D1 AND STORE RESULT IN 
        SUB.B   #$1, D1                 ;SUBTRACTING A BYTE SIZE DATA FROM D1 AND STORE RESULT IN D1
        
        SUB.W   D0, D1                  ;SUBTRACTING D0 FROM D1, THEN STORE RESULT IN D1
        SUB.W   A0, D1                  ;SUBTRACTING DATA STORING IN A0 FROM D1 AND STORE RESULT IN D1
        SUB.W   (A0), D1                ;SUBTRACTING DATA THAT A0 POINTS TO FROM D1 AND STORE RESULT IN D1
        SUB.W   (A0)+, D1               ;SUBTRACTING DATA THAT A0 POINTS TO FROM D1 AND STORE RESULT IN D1, THEN INCREMENT THE POINTER
        SUB.W   -(A0), D1               ;DECREMENT THE POINTER, THEN SUBTRACTING DATA THAT A0 POINTS TO FROM D1 AND STORE RESULT IN D1
        SUB.W   $4000, D1               ;SUBTRACTING DATA AT THE GIVEN ADDRESS FROM D1 AND STORE RESULT IN 
        SUB.W   $00004000, D1           ;SUBTRACTING DATA AT THE GIVEN ADDRESS FROM D1 AND STORE RESULT IN 
        SUB.W   #$12, D1                ;SUBTRACTING A WORD SIZE DATA FROM D1 AND STORE RESULT IN D1
        
        SUB.L   D0, D1                  ;SUBTRACTING D0 FROM D1, THEN STORE RESULT IN D1
        SUB.L   A0, D1                  ;SUBTRACTING DATA STORING IN A0 FROM D1 AND STORE RESULT IN D1
        SUB.L   (A0), D1                ;SUBTRACTING DATA THAT A0 POINTS TO FROM D1 AND STORE RESULT IN D1
        SUB.L   (A0)+, D1               ;SUBTRACTING DATA THAT A0 POINTS TO FROM D1 AND STORE RESULT IN D1, THEN INCREMENT THE POINTER
        SUB.L   -(A0), D1               ;DECREMENT THE POINTER, THEN SUBTRACTING DATA THAT A0 POINTS TO FROM D1 AND STORE RESULT IN D1
        SUB.L   $4000, D1               ;SUBTRACTING DATA AT THE GIVEN ADDRESS FROM D1 AND STORE RESULT IN 
        SUB.L   $00004000, D1           ;SUBTRACTING DATA AT THE GIVEN ADDRESS FROM D1 AND STORE RESULT IN 
        SUB.L   #$1234, D1              ;SUBTRACTING A LONG SIZE DATA FROM D1 AND STORE RESULT IN D1
            
*------------ FIXED SOURCE: DESTINATION OPERAND ------------

        SUB.B   D0, (A0)                ;SUBTRACTING D0 FROM DATA THAT A0 POINTS TO, THEN STORE RESULT IN ADDRESS A0 POINTS TO
        SUB.B   D0, (A0)+               ;SUBTRACTING D0 FROM DATA THAT A0 POINTS TO, STORE RESULT IN THE ADDRESS A0 POINT TO AND INCREMENT POINTER 
        SUB.B   D0, -(A0)               ;SUBTRACTING D0 FROM DATA THAT A0 POINTS TO, DECREMENT POINTER, THEN STORE RESULT IN THAT ADDRESS 
        SUB.B   D0, $4000               ;SUBTRACTING D0 FROM DATA AT THE GIVEN ADDRESS WITH D1 AND STORE RESULT IN THAT ADDRESS
        SUB.B   D0, $00004000           ;SUBTRACTING D0 FROM DATA AT THE GIVEN ADDRESS WITH D1 AND STORE RESULT IN THAT ADDRESS
        
        SUB.W   D0, (A0)                ;SUBTRACTING D0 FROM DATA THAT A0 POINTS TO, THEN STORE RESULT IN ADDRESS A0 POINTS TO
        SUB.W   D0, (A0)+               ;SUBTRACTING D0 FROM DATA THAT A0 POINTS TO, STORE RESULT IN THE ADDRESS A0 POINT TO AND INCREMENT POINTER 
        SUB.W   D0, -(A0)               ;SUBTRACTING D0 FROM DATA THAT A0 POINTS TO, DECREMENT POINTER, THEN STORE RESULT IN THAT ADDRESS 
        SUB.W   D0, $4000               ;SUBTRACTING D0 FROM DATA AT THE GIVEN ADDRESS WITH D1 AND STORE RESULT IN THAT ADDRESS
        SUB.W   D0, $00004000           ;SUBTRACTING D0 FROM DATA AT THE GIVEN ADDRESS WITH D1 AND STORE RESULT IN THAT ADDRESS
        
        SUB.L   D0, (A0)                ;SUBTRACTING D0 FROM DATA THAT A0 POINTS TO, THEN STORE RESULT IN ADDRESS A0 POINTS TO
        SUB.L   D0, (A0)+               ;SUBTRACTING D0 FROM DATA THAT A0 POINTS TO, STORE RESULT IN THE ADDRESS A0 POINT TO AND INCREMENT POINTER 
        SUB.L   D0, -(A0)               ;SUBTRACTING D0 FROM DATA THAT A0 POINTS TO, DECREMENT POINTER, THEN STORE RESULT IN THAT ADDRESS 
        SUB.L   D0, $4000               ;SUBTRACTING D0 FROM DATA AT THE GIVEN ADDRESS WITH D1 AND STORE RESULT IN THAT ADDRESS
        SUB.L   D0, $00004000           ;SUBTRACTING D0 FROM DATA AT THE GIVEN ADDRESS WITH D1 AND STORE RESULT IN THAT ADDRESS
        
        
*-----------------------------------------------------------
* **********************  SUBA TEST  ***********************
*-----------------------------------------------------------
        SUBA.W   D0,A1                  ;SUBTRACTING D0 FROM D1, THEN STORE RESULT IN D1
        SUBA.W   A0,A1                  ;SUBTRACTING DATA STORING IN A0 FROM D1 AND STORE RESULT IN D1
        SUBA.W   (A0),A1                ;SUBTRACTING DATA THAT A0 POINTS TO FROM D1 AND STORE RESULT IN D1
        SUBA.W   (A0)+,A1               ;SUBTRACTING DATA THAT A0 POINTS TO FROM D1 AND STORE RESULT IN D1, THEN INCREMENT THE POINTER
        SUBA.W   -(A0),A1               ;DECREMENT THE POINTER, THEN SUBTRACTING DATA THAT A0 POINTS TO FROM D1 AND STORE RESULT IN D1
        SUBA.W   $4000,A1               ;SUBTRACTING DATA AT THE GIVEN ADDRESS FROM D1 AND STORE RESULT IN 
        SUBA.W   $00004000,A1           ;SUBTRACTING DATA AT THE GIVEN ADDRESS FROM D1 AND STORE RESULT IN 
        SUBA.W   #$12,A1                ;SUBTRACTING A WORD SIZE DATA FROM D1 AND STORE RESULT IN D1
        
        SUBA.L   D0,A1                  ;SUBTRACTING D0 FROM D1, THEN STORE RESULT IN D1
        SUBA.L   A0,A1                  ;SUBTRACTING DATA STORING IN A0 FROM D1 AND STORE RESULT IN D1
        SUBA.L   (A0),A1                ;SUBTRACTING DATA THAT A0 POINTS TO FROM D1 AND STORE RESULT IN D1
        SUBA.L   (A0)+,A1               ;SUBTRACTING DATA THAT A0 POINTS TO FROM D1 AND STORE RESULT IN D1, THEN INCREMENT THE POINTER
        SUBA.L   -(A0),A1               ;DECREMENT THE POINTER, THEN SUBTRACTING DATA THAT A0 POINTS TO FROM D1 AND STORE RESULT IN D1
        SUBA.L   $4000,A1               ;SUBTRACTING DATA AT THE GIVEN ADDRESS FROM D1 AND STORE RESULT IN 
        SUBA.L   $00004000,A1           ;SUBTRACTING DATA AT THE GIVEN ADDRESS FROM D1 AND STORE RESULT IN 
        SUBA.L   #$1234,A1              ;SUBTRACTING A LONG SIZE DATA FROM D1 AND STORE RESULT IN D1
        
*-----------------------------------------------------------
* **********************  SUBI TEST  ***********************
*-----------------------------------------------------------
        SUBI.B  #$3B,D0
        SUBI.B  #$3B,(A0)
        SUBI.B  #$3B,(A0)+
        SUBI.B  #$3B,-(A0)
        ;
        ;
        ;
        ;

        
    END    START





*~Font name~Courier New~
*~Font size~10~
*~Tab type~1~
*~Tab size~4~
